<UserControl x:Class="DCRManagement.UI.Views.DCRImagePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#FFF4F7FB">
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="*" />
            <RowDefinition Height="66" />
        </Grid.RowDefinitions>
        <DockPanel Background="White" LastChildFill="False">
            <TextBlock x:Name="TitleText" Text="Images" FontWeight="SemiBold" Margin="12,0,0,0" VerticalAlignment="Center" DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="0,4,10,4">
                <Button Content="Add" MinWidth="58" Margin="0,0,6,0" />
                <Button Content="Crop" MinWidth="58" />
            </StackPanel>
        </DockPanel>
        <Border Grid.Row="1" Margin="12" BorderBrush="#FFAAB6C5" BorderThickness="1" CornerRadius="8" Background="#FFEDF2F8">
            <TextBlock Text="Image viewer" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" />
        </Border>
        <ItemsControl x:Name="ThumbnailList" Grid.Row="2" Margin="12,0,12,12" />
    </Grid>
</UserControl>
